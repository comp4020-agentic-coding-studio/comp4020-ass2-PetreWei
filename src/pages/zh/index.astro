---
import Card from "astro-theme-university/components/Card.astro";
import CardGrid from "astro-theme-university/components/CardGrid.astro";
import ContentLayout from "astro-theme-university/layouts/ContentLayout.astro";
import LangSwitch from "../../components/LangSwitch.astro";
import heroImage from "../../assets/images/hero-home.avif";
import { courseMeta } from "../../course-config";
import { siteConfig, zhLinks } from "../../site-config";

// Pilot translation: only the home page and nav are translated so far, so
// the tags below are a display-only Chinese gloss of courseMeta.tags rather
// than a second source of truth — the English tags in course-config.ts
// remain the ones spec/course-promises.test.ts checks.
const tagLabels: Record<string, string> = {
  retries: "重试",
  backoff: "退避",
  outages: "宕机",
};
---

<ContentLayout
  title={`${courseMeta.code}：稍后重试`}
  description="一个决定，每周换一种处境重新问一遍：出错了—该不该重试？又该怎么重试？从时好时坏的 HTTP 客户端，到同一笔钱被扣两次的支付流程，我们要弄清楚的是：执着于重试，什么时候真能解决问题，什么时候只是悄悄把事情推得更糟。"
  heroImage={heroImage}
  heroImageAlt="同一只手反复按下同一个按钮，五道印痕一次比一次错开得更远；米色纸上以金、黑两色套印的孔版画"
  {...siteConfig}
  links={zhLinks}
>
  <LangSwitch href="/" label="View in English" />

  <ul class="course-tags" aria-label="Course tags">
    {courseMeta.tags.map((tag) => <li>{tagLabels[tag] ?? tag}</li>)}
  </ul>

  <p class="thesis">
    答案通常是“该重试”。危险恰恰在这里：重试看着像是尽责，也几乎每次都能奏效，可它从来不是免费的。这门课里的每一种解法都是一笔交易—换来一些东西，同时也花掉另一些东西；而要练的本事只有一样：付钱之前，先把价码报出来。
  </p>

  <h2>十二周，十二种处境</h2>
  <p>
    不是十二个主题，而是同一个问题，换十二种处境来问；每一种都具体得可以照着复现：一次读操作超时；一笔钱被扣了两次；一万个客户端在同一秒集体重连；凌晨三点，两个人各自把同一笔款打了出去，中间没有一行代码拦着。
  </p>
  <p>
    每周的收尾也是同一套路：先给解法，再算代价。退避（backoff）替快撑不住的下游服务争来一口气，花掉的是你的延迟；熔断器（circuit
    breaker）主动让出本来还能拿到的可用性；截止时间（deadline）会把眼看就要成功的活儿直接丢掉。到第十二周，你应该已经再也提不出一条不谈代价的重试策略—这门课要教的，从头到尾就只有这一件事。
  </p>

  <h2>这门课适合谁</h2>
  <p>
    三年级学生：写过要走网络的代码，也已经被网络坑过一次。你要能读懂一份陌生的代码库，并在上面改动几行；但不需要分布式系统的理论底子，这门课也不假定你在生产环境里跑过什么东西。
  </p>
  <p>
    这学期，你大半时间都在故意把东西弄坏，然后决定拿它怎么办。学期结束时，你会带走三样东西：一份审计报告；一条重试策略—你要当面为它辩护，而对手是专门被派来拆台的人；还有一篇文章，论证这一次根本就不该重试。
  </p>

  <h2>接下来看什么</h2>
  <CardGrid columns={2}>
    <Card title="实验" href="/sessions/">
      <p>十二种处境，按周次排开，以及每一种要付的代价。（该页暂为英文）</p>
    </Card>
    <Card title="考核" href="/assessments/">
      <p>一份审计、一条要你当面辩护的策略，还有一份要你论证的主张。各占多少分，什么时候交。（该页暂为英文）</p>
    </Card>
    <Card title="教学团队" href="/people/">
      <p>谁在教这门课，找他们时该带上什么，以及什么时候能找到人。（该页暂为英文）</p>
    </Card>
  </CardGrid>
</ContentLayout>

<style>
  .course-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-block: 0 2rem;
    padding: 0;
    list-style: none;
  }

  .thesis {
    font-size: 1.15rem;
    line-height: 1.55;
    margin-block: 0 2.5rem;
    padding-inline-start: 1rem;
    border-inline-start: 3px solid var(--at-accent);
  }

  .course-tags li {
    padding: 0.25rem 0.65rem;
    border-radius: 999px;
    color: var(--at-text-secondary);
    background: var(--at-bg-alt);
    font-size: 0.9rem;
  }
</style>
